apiVersion: v1
kind: Service
metadata:
  name: {{ include "hdr-empire.fullname" . }}
  labels:
    {{- include "hdr-empire.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hdr-empire.selectorLabels" . | nindent 4 }}
