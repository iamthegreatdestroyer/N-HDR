apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hdr-empire.serviceAccountName" . }}
  labels:
    {{- include "hdr-empire.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
